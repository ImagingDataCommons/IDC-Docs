#### 5.1.6 BigQuery

* Ensure that the Service Account which will run the Web Application has access to any relevant datasets. If the datasets are located in another project, the runtime service account must be granted access on a per dataset basis.